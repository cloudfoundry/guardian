package nerd_test

import (
	. "github.com/onsi/ginkgo"
)

// Not sure why we need this, perhaps a ginkgo limitation that we need a non GOOS specific test file in this package
var _ = Describe("Nerd Cross-Platform", func() {})
