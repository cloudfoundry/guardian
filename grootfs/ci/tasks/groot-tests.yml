---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfgarden/grootfs-ci
    tag: latest

inputs:
  - name: grootfs-git-repo
    path: src/code.cloudfoundry.org/grootfs

params:
  REGISTRY_USERNAME:
  REGISTRY_PASSWORD:
  VOLUME_DRIVER:

run:
  path: src/code.cloudfoundry.org/grootfs/ci/scripts/groot-tests.sh
  user: root
